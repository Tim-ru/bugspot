# BugSpot - Quick Start Guide для пилотов

Добро пожаловать в BugSpot! Это руководство поможет вам быстро начать работу с системой.

## Что такое BugSpot?

BugSpot - это виджет для сбора отчетов об ошибках от пользователей вашего сайта. Он автоматически собирает:
- Скриншоты страницы
- Информацию о браузере и окружении
- URL и другие технические детали

## Шаг 1: Регистрация

1. Откройте BugSpot Dashboard: `https://your-domain.com`
2. Нажмите "Register" (Регистрация)
3. Введите email и пароль
4. После регистрации вы автоматически войдете в систему

## Шаг 2: Получение API ключа

1. После входа перейдите в **Profile** (Профиль)
2. Найдите ваш **User API Key** - это ваш личный ключ
3. Также создается **Default Project** с собственным API ключом

## Шаг 3: Получение Project API Key

1. Перейдите в **Dashboard** → **Projects**
2. Выберите проект (или создайте новый)
3. Скопируйте **Project API Key** - это ключ для виджета

⚠️ **Важно**: Project API Key используется в виджете для отправки баг-репортов.

## Шаг 4: Интеграция виджета

### Простая интеграция (HTML)

Добавьте этот код в `<head>` вашего HTML:

```html
<script src="https://your-domain.com/widget.js"></script>
<script>
  window.bugSpotConfig = {
    apiKey: 'YOUR_PROJECT_API_KEY',  // Замените на ваш Project API Key
    apiUrl: 'https://your-domain.com',
    position: 'bottom-right',
    primaryColor: '#3B82F6'
  };
</script>
```

### Интеграция с React

```jsx
import { useEffect } from 'react';

function App() {
  useEffect(() => {
    // Загрузка виджета
    const script = document.createElement('script');
    script.src = 'https://your-domain.com/widget.js';
    script.onload = () => {
      if (window.BugSpot) {
        window.BugSpot.init({
          apiKey: process.env.REACT_APP_BUGSPOT_API_KEY,
          apiUrl: 'https://your-domain.com',
          position: 'bottom-right'
        });
      }
    };
    document.head.appendChild(script);

    return () => {
      // Очистка при размонтировании
      document.head.removeChild(script);
    };
  }, []);

  return <div>Your App</div>;
}
```

### Интеграция с Next.js

```tsx
// app/layout.tsx или pages/_app.tsx
import Script from 'next/script';

export default function Layout({ children }) {
  return (
    <>
      <Script
        id="bugspot-config"
        strategy="beforeInteractive"
        dangerouslySetInnerHTML={{
          __html: `
            window.bugSpotConfig = {
              apiKey: '${process.env.NEXT_PUBLIC_BUGSPOT_API_KEY}',
              apiUrl: 'https://your-domain.com',
              position: 'bottom-right'
            };
          `,
        }}
      />
      <Script
        src="https://your-domain.com/widget.js"
        strategy="afterInteractive"
      />
      {children}
    </>
  );
}
```

## Шаг 5: Тестирование

1. Откройте ваш сайт с интегрированным виджетом
2. В правом нижнем углу должна появиться кнопка виджета
3. Нажмите на кнопку
4. Заполните форму:
   - **Title**: Краткое описание проблемы
   - **Description**: Подробное описание
   - **Severity**: Уровень критичности
   - **Email**: Ваш email (опционально)
5. Нажмите "Submit Report"
6. Вы должны увидеть сообщение об успешной отправке

## Шаг 6: Просмотр баг-репортов

1. Вернитесь в BugSpot Dashboard
2. Перейдите в раздел **Bug Reports**
3. Вы увидите все отправленные баг-репорты
4. Кликните на любой репорт для просмотра деталей:
   - Скриншот
   - Информация о браузере
   - URL страницы
   - Другие технические детали

## Настройка виджета

Вы можете настроить виджет через конфигурацию:

```javascript
window.bugSpotConfig = {
  apiKey: 'YOUR_PROJECT_API_KEY',
  apiUrl: 'https://your-domain.com',
  
  // Позиция виджета
  position: 'bottom-right', // 'bottom-right', 'bottom-left', 'top-right', 'top-left'
  
  // Цветовая схема
  primaryColor: '#3B82F6',
  
  // Функциональность
  enableScreenshot: true,    // Автоматический скриншот
  showPreview: true,        // Показывать предпросмотр скриншота
  autoInit: true            // Автоматическая инициализация
};
```

## Управление проектами

### Создание нового проекта

1. Перейдите в **Dashboard** → **Projects**
2. Нажмите "Create New Project"
3. Введите название проекта
4. Опционально укажите домен
5. Скопируйте новый **Project API Key**

### Настройки проекта

Каждый проект имеет свой API ключ. Это позволяет:
- Разделять баг-репорты по проектам
- Использовать разные настройки для разных сайтов
- Управлять доступом

## Фильтрация и поиск баг-репортов

В Dashboard вы можете:

- **Поиск**: Используйте поисковую строку для поиска по заголовку, описанию или тегам
- **Фильтр по статусу**: Open, In Progress, Resolved, Closed
- **Фильтр по severity**: Low, Medium, High, Critical
- **Сортировка**: По дате создания (новые сначала)

## Изменение статуса баг-репорта

1. Откройте баг-репорт
2. В правой панели выберите новый статус:
   - **Open**: Новая проблема
   - **In Progress**: В работе
   - **Resolved**: Исправлено
   - **Closed**: Закрыто

## Экспорт данных

1. В Dashboard нажмите кнопку **Export Data**
2. Все баг-репорты будут экспортированы в JSON формате
3. Вы можете использовать эти данные для анализа или интеграции с другими системами

## Часто задаваемые вопросы

### Q: Где найти API ключи?

**A**: 
- **User API Key**: Profile → User API Key
- **Project API Key**: Dashboard → Projects → выберите проект → Project API Key

### Q: Виджет не появляется на странице

**A**: Проверьте:
1. Правильно ли указан `apiKey` в конфигурации
2. Загружается ли `widget.js` (проверьте консоль браузера)
3. Нет ли ошибок JavaScript в консоли

### Q: Баг-репорты не отправляются

**A**: Проверьте:
1. Правильность `apiKey` (должен быть Project API Key, не User API Key)
2. Правильность `apiUrl`
3. Консоль браузера на наличие ошибок
4. Network tab в DevTools - проверьте запрос к `/api/bug-reports/submit`

### Q: Как изменить позицию виджета?

**A**: Измените параметр `position` в конфигурации:
```javascript
position: 'bottom-left' // или 'top-right', 'top-left'
```

### Q: Можно ли отключить скриншоты?

**A**: Да, установите `enableScreenshot: false` в конфигурации.

### Q: Как работает fallback на localStorage?

**A**: Если API недоступен, виджет автоматически сохраняет отчеты в localStorage браузера. При следующей доступности API они будут отправлены автоматически.

## Получение помощи

Если у вас возникли проблемы:

1. Проверьте [документацию](README.md)
2. Проверьте [руководство по интеграции](INTEGRATION_GUIDE.md)
3. Проверьте [API документацию](API.md)
4. Создайте issue в репозитории или свяжитесь с поддержкой

## Следующие шаги

После успешной интеграции:

1. ✅ Протестируйте виджет на вашем сайте
2. ✅ Настройте фильтры и уведомления
3. ✅ Изучите аналитику в Dashboard
4. ✅ Настройте автоматические действия (если доступно)

Удачи с BugSpot! 🚀


